# 更新日志 - V2.6

## 发布日期
2026-05-05

## 版本概述
V2.6 版本主要增强了 IP 备注信息功能，并添加了可自定义编辑的数据源管理功能。

## 新增功能

### 1. IP 备注信息增强 ✨
- **显示格式**: `延迟 ms(数据源 - 线路类型)`
- **示例**: `1.2.3.4#36ms(164746-电信)`
- 在 IP 列表中直观显示每个 IP 的详细信息
- 复制功能支持复制带备注的 IP 地址

### 2. 数据源管理功能 🌐
- **可视化管理界面**: 通过 Web 界面管理数据源
- **CRUD 操作**: 
  - ➕ 添加新的数据源
  - ✏️ 编辑数据源的 URL、名称、线路类型
  - 🗑️ 删除无效数据源
  - 👁️/🚫 启用/禁用数据源
- **即时生效**: 保存配置后下次更新立即使用新配置
- **线路分类**: 支持电信、联通、移动三种线路类型标记

### 3. 后端 API 增强 🔌
- `GET /sources` - 获取数据源配置
- `POST /sources` - 保存数据源配置
- `GET /raw-ips-with-sources` - 获取带来源信息的 IP 数据

### 4. 数据结构优化 📦
- `cloudflare_ips` KV 存储新增 `ipSources` 字段
- `cloudflare_fast_ips` 中的每个 IP 包含 `sources` 数组
- 完整的来源追溯能力

## 改进内容

### 前端显示改进
- IP 列表项显示完整的延迟、数据源、线路信息
- 复制按钮支持一键复制带备注的 IP
- 数据源管理模态框，友好的编辑界面
- 响应式设计，移动端友好

### 后端逻辑优化
- 支持动态加载自定义数据源
- IP 收集时自动记录来源信息
- 测速结果关联来源数据
- 向后兼容旧数据结构

## 技术细节

### 文件变更
- `_workers.js`: 主要功能实现文件
  - 新增 ~300 行代码
  - 修改 ~15 处关键逻辑
  - 添加 3 个新 API 端点
  - 添加 1 个模态框界面
  - 添加 8 个 JavaScript 函数

### 核心函数
1. `handleSources()` - 数据源管理 API 处理
2. `getSourcesConfig()` - 获取数据源配置
3. `handleRawIPsWithSources()` - 带来源的 IP 数据 API
4. `updateAllIPs()` - 重写，支持自定义数据源和来源记录
5. `autoSpeedTestAndStore()` - 增强，包含来源信息
6. `openSourcesModal()` - 打开数据源管理界面
7. `loadSourcesList()` - 加载数据源列表
8. `renderSourcesList()` - 渲染数据源列表
9. `saveSourcesConfig()` - 保存数据源配置
10. `copyIPWithNote()` - 复制带备注的 IP

### 样式新增
- `.source-item` - 数据源项样式
- `.source-info` - 数据源信息容器
- `.source-type` - 线路类型标签
- `.source-actions` - 操作按钮组
- `.source-item.disabled` - 禁用状态

## 默认数据源

系统预置 7 个优质数据源：
- 3 个电信线路
- 2 个联通线路
- 2 个移动线路

## 兼容性

### ✅ 完全向后兼容
- 旧版 KV 数据自动适配
- 未配置数据源时使用默认列表
- API 端点向后兼容

### 升级要求
- Cloudflare Workers 运行环境
- KV 命名空间 `IP_STORAGE` 绑定
- 管理员密码（可选，但推荐）

## 已知限制

1. 数据源管理需要管理员权限
2. 修改数据源后需要重新更新 IP 才能生效
3. IP 来源信息仅在首次收集时记录

## 升级步骤

1. **备份**: 备份当前 Worker 代码和 KV 数据
2. **部署**: 上传新版 `_workers.js`
3. **验证**: 确认基本功能正常
4. **配置**: （可选）配置自定义数据源
5. **更新**: 执行 IP 更新操作
6. **测试**: 验证备注信息显示

## 问题反馈

如遇到问题，请检查：
1. KV 绑定是否正确
2. 管理员权限是否足够
3. 浏览器控制台是否有错误
4. 数据源 URL 是否可访问

## 下一版本计划

- [ ] 批量导入/导出数据源配置
- [ ] 数据源健康检查
- [ ] 按线路类型筛选 IP
- [ ] 导出带备注的 IP 列表

---

**统计**:
- 代码行数：2749 行
- 新增功能：4 个
- 新增 API：3 个
- 新增函数：12 个
- 向后兼容：✅
